import React from "react";
import { Link } from "react-router-dom";

const NotFound = () => {
  const style = {
    margin: "4rem auto",
    color: "inherit",
    textAlign: "center",
  };
  return (
    <div className="cocktail-details">
      {/*Implement a simple div here with heading coctail not found and a button 
      linking back to home page . Use the style mentioned above and the div classname "cocktail-detail"*/}
    </div>
  );
};

export default NotFound;
